# 1. Unstage everything immediately to undo the "git add ." mistake
git reset

# 2. Build the strict, non-negotiable platform ignore configuration file
cat << 'EOF' > .gitignore
node_modules/
package-lock.json
.wrangler/
dist/
EOF

# 3. Pull and install NVM using the fully qualified canonical URL string
curl -o- https://githubusercontent.com | bash

# 4. Source the native NVM binary straight into your current shell context
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"

# 5. Install and use the Node.js v22 runtime ecosystem required by Wrangler
nvm install 22
nvm use 22

# 6. Securely stage only the authorized structural tracking codebases
git add .gitignore package.json bin/ profiles/ tests/ convert.sh

# 7. Execute the edge worker route configuration sync pass
npx wrangler worker deploy --var PEOPLE_MANIFEST="$(cat dist/people.json)"

